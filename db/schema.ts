// Schema placeholder. No tables yet — this phase ships the Coming Soon
// page only. Real tables (architects, projects, clients, ...) land in
// the next phase once business functionality is scoped.
export {};
